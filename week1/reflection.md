Going through the last commit on one of my repos (42_Python_Projects) I learned about how can I use `git cat-file` to view the contents of the commit at HEAD, from there I managed to access the commit's tree contents using ^{tree} syntax, which showed me all the sub-tree entries stored there, I've chosen one and went down the tree hierarchy, which in the end gave me multiple blobs that I viewed their contents, the things I found interesting is the idea of compressing the data and then store it in binary format in blobs is trivial but I still think there isn't so much difference between git and linux filesystems in that sense, I wonder how git achieve small repos compared to storing multiple versions of the files, there should be a mechanism that utilizes any differential encoding scheme to compress the data even more, I'll research that next ^_^

![Git relog](~/Pictures/screenshot-2026-05-14_00-28-40.png)

